import React from 'react'
import TermComp from '../../../components/TermComp'

const Terms = ({visible,setVisibility}) => {
    return (
        <TermComp visible={visible} setVisibility={setVisibility}/>
    )
}

export default Terms
